defmodule AdventOfCode.Y<%= year %>.Day<%= String.pad_leading(day, 2, "0") %>Test do
  @moduledoc false

  use ExUnit.Case
  @moduletag :y<%= year |> String.split_at(2) |> elem(1) %><%= String.pad_leading(day, 2, "0") %>

  alias AdventOfCode.Y<%= year  %>.Day<%= String.pad_leading(day, 2, "0") %>, as: Solution

  describe "part 1" do
    test "example" do
      data = ~S"""
      """

      assert Solution.run(data, 1) == nil
    end

    test "input file" do
      assert Solution.run(1) == nil
    end
  end

  describe "part 2" do
    test "example" do
      data = ~S"""
      """

      assert Solution.run(data, 2) == nil
    end

    test "input file" do
      assert Solution.run(2) == nil
    end
  end
end
